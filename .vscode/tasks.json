{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build LaTeX with bibliography",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "command": "pdflatex report && bibtex report && pdflatex report && pdflatex report",
            "options": {
                "cwd": "report"
            }
        }
    ]
}
